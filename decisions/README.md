# Architecture Decisions

Use an ADR when a change affects deployment, protocol, rendering, provider
boundaries, privacy, license, persistence, or repository workflow. Local and
reversible implementation choices belong in a PR note instead.
